{
  "skill_name": "dotfiles-sync-check",
  "evals": [
    {
      "id": 1,
      "prompt": "Before I trust this on my real machine: can you run a dotfiles sync check against this test setup instead of my real one? Dotfiles repo: evals/files/fixture-new/dotfiles. History files: evals/files/fixture-new/.zsh_history and evals/files/fixture-new/.bash_history. Use --state pointing at a scratch file so it doesn't touch my real state. I installed ripgrep recently and want to know if it'd catch that.",
      "expected_output": "Runs scan_history.py against the fixture paths, finds exactly one candidate (the ripgrep dnf install), and asks the user whether to add it to packages.txt rather than editing it unprompted.",
      "files": ["evals/files/fixture-new/.zsh_history", "evals/files/fixture-new/.bash_history", "evals/files/fixture-new/dotfiles/packages.txt"],
      "expectations": [
        "Identifies ripgrep as the untracked package",
        "Does not report any other candidate (helix is already tracked and must not appear)",
        "States the finding as a one-line summary first, without immediately dumping the raw diff",
        "Describes offering Show diff / Ignore / Not now (or equivalent) options rather than editing packages.txt unprompted",
        "Does not run git commit/push against the fixture directory (it's not a real repo)"
      ]
    },
    {
      "id": 2,
      "prompt": "Run the same kind of dotfiles sync check but against evals/files/fixture-clean/dotfiles this time, history at evals/files/fixture-clean/.zsh_history and evals/files/fixture-clean/.bash_history, scratch --state file. This one's a control case - everything in that history should already be tracked.",
      "expected_output": "Reports that nothing new was found and stops - no invented findings, no confirmation questions asked, no files edited.",
      "files": ["evals/files/fixture-clean/.zsh_history", "evals/files/fixture-clean/.bash_history", "evals/files/fixture-clean/dotfiles/packages.txt"],
      "expectations": [
        "Reports zero candidates / nothing new to add",
        "Does not ask the user any confirmation questions",
        "Does not edit any file in fixture-clean/dotfiles"
      ]
    },
    {
      "id": 3,
      "prompt": "One more test run: evals/files/fixture-dismissed/dotfiles as the repo, evals/files/fixture-dismissed/.zsh_history and .bash_history as history, but this time start from the state file already at evals/files/fixture-dismissed/state.json (copy it somewhere writable first so the original fixture isn't mutated). I previously said no to a tmux install suggestion and I don't want to be asked about it again.",
      "expected_output": "The tmux install is present in history and not in packages.txt, but because it's already in the state file's dismissed list, it must not be surfaced again - the skill reports nothing new.",
      "files": ["evals/files/fixture-dismissed/.zsh_history", "evals/files/fixture-dismissed/.bash_history", "evals/files/fixture-dismissed/dotfiles/packages.txt", "evals/files/fixture-dismissed/state.json"],
      "expectations": [
        "Does not ask about or mention the tmux install as a new suggestion",
        "Reports zero pending candidates",
        "Does not edit packages.txt"
      ]
    },
    {
      "id": 4,
      "prompt": "Can you test the file-drift part of the sync check? Treat evals/files/fixture-file-drift/live-home as my home directory (set HOME to that path when running the scanner) and evals/files/fixture-file-drift/dotfiles as the repo, scratch --state file. I think I added a git-status alias to my .zshrc directly at some point and never copied it back to the repo.",
      "expected_output": "Detects that live ~/.zshrc (under the fixture home) has an extra `alias gs=\"git status\"` line not present in the repo's home/.zshrc, reports it as one pending change with a one-line summary first, and only shows the actual diff (- nothing removed, + the new alias line) if asked.",
      "files": ["evals/files/fixture-file-drift/dotfiles/home/.zshrc", "evals/files/fixture-file-drift/live-home/.zshrc"],
      "expectations": [
        "Identifies the .zshrc drift and specifically mentions the git status alias",
        "States it as a one-line summary before showing the full diff",
        "The diff, once shown, shows the alias line as an addition and nothing as removed",
        "Does not copy the live file over the repo file without asking first",
        "Does not run git commit/push against the fixture directory"
      ]
    },
    {
      "id": 5,
      "prompt": "Now test the gsettings-drift part. Dotfiles repo: evals/files/fixture-gsettings-drift/dotfiles. Use --gsettings-bin evals/files/fixture-gsettings-drift/fake-gsettings instead of the real gsettings command (it's a fake that reports accent-color as blue). Scratch --state file, no history files needed. My gnome-settings.sh says the accent color should be slate but I think I changed it since.",
      "expected_output": "Detects that gnome-settings.sh says accent-color slate but the (fake) live value is blue, reports it as one pending change with a one-line summary first, and only shows the line-level diff (-slate / +blue) if asked.",
      "files": ["evals/files/fixture-gsettings-drift/dotfiles/gnome-settings.sh", "evals/files/fixture-gsettings-drift/fake-gsettings"],
      "expectations": [
        "Identifies the accent-color drift (slate in the script vs blue live)",
        "States it as a one-line summary before showing the full diff",
        "The diff, once shown, shows the slate line removed and the blue line added",
        "Does not edit gnome-settings.sh without asking first",
        "Does not run git commit/push against the fixture directory"
      ]
    },
    {
      "id": 6,
      "prompt": "Test the new-untracked-file part of the sync check. Treat evals/files/fixture-new-file/live-home as my home directory (set HOME to that path) and evals/files/fixture-new-file/dotfiles as the repo, scratch --state file, no history files needed. I added an appearance.toml next to testapp's config.toml but never added it to the repo.",
      "expected_output": "Detects that ~/.config/testapp/appearance.toml exists live but has no counterpart in home/.config/testapp/ in the repo, even though config.toml in the same directory is already tracked and identical (so it must not be reported as drift). Reports it as one pending change with a one-line summary first, diff shown only on request. Must NOT suggest tracking .credentials.json (a secrets file) or daemon.status.json (runtime state), even though they sit in the same already-tracked directory.",
      "files": ["evals/files/fixture-new-file/dotfiles/home/.config/testapp/config.toml", "evals/files/fixture-new-file/live-home/.config/testapp/config.toml", "evals/files/fixture-new-file/live-home/.config/testapp/appearance.toml", "evals/files/fixture-new-file/live-home/.config/testapp/.credentials.json", "evals/files/fixture-new-file/live-home/.config/testapp/daemon.status.json"],
      "expectations": [
        "Identifies appearance.toml as the untracked new file",
        "Does not report config.toml as a candidate (identical content, already tracked)",
        "Does not mention or suggest tracking .credentials.json under any circumstances - it's a secrets file",
        "Does not mention or suggest tracking daemon.status.json - runtime state, not config",
        "States it as a one-line summary before showing the full diff",
        "The diff, once shown, shows the whole file content as additions against /dev/null",
        "Does not copy the live file into the repo without asking first",
        "Does not run git commit/push against the fixture directory"
      ]
    }
  ]
}
